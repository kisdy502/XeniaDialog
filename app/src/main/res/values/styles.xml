<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:listDivider">@drawable/divider_bg</item>
    </style>

    <style name="AppTheme.Launcher">
        <item name="android:windowBackground">@drawable/launcher_bg</item>
    </style>

    <style name="common_transparent_dialog" parent="@android:style/Theme">
        <!-- 设置是否透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 背景 -->
        <item name="android:background">@null</item>
        <!-- 窗口背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>

        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="dialogWindowAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/left_dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/left_dialog_exit_anim</item>
    </style>

    <style name="dialogWindowAnim2" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/right_dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/right_dialog_exit_anim</item>
    </style>

    <style name="dialogWindowAnim3" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/bottom_dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/bottom_dialog_exit_anim</item>
    </style>



</resources>
